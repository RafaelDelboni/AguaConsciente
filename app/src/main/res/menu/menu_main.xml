<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
        tools:context=".MainActivity"
>
    <!-- Timers, should appear as action button -->
    <item android:id="@+id/action_timer"
        android:icon="@drawable/ic_timer_set"
        android:title="@string/action_timer"
        app:showAsAction="ifRoom" />
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_reports"
        android:icon="@drawable/ic_reports"
        android:title="@string/action_reports"
        app:showAsAction="ifRoom" />
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never" />
    <item android:id="@+id/action_about"
        android:title="@string/action_about"
        app:showAsAction="never" />
</menu>
